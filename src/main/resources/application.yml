spring:
  application:
    name: ajouway

  profiles:
    default: dev
    group:
      test: h2-test
      dev: postgresql-dev

  config:
    import: "application-db.yml"

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace